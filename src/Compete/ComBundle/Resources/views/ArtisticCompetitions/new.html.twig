{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ArtisticCompetitions creation</h1>

    <form action="{{ path('artisticcompetitions_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('artisticcompetitions') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}

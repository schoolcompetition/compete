{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TournamentQuestions list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Question</th>
                <th>Type</th>
                <th>Approved</th>
                <th>Isactive</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tournamentquestions_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.question }}</td>
                <td>{{ entity.type }}</td>
                <td>{{ entity.approved }}</td>
                <td>{{ entity.isActive }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('tournamentquestions_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tournamentquestions_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tournamentquestions_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}

{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Schools list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Address</th>
                <th>City</th>
                <th>Country</th>
                <th>Password</th>
                <th>Picture</th>
                <th>Email</th>
                <th>Website</th>
                <th>Reported</th>
                <th>Legalpaperscan</th>
                <th>Isactive</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('schools_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.address }}</td>
                <td>{{ entity.city }}</td>
                <td>{{ entity.country }}</td>
                <td>{{ entity.password }}</td>
                <td>{{ entity.picture }}</td>
                <td>{{ entity.email }}</td>
                <td>{{ entity.website }}</td>
                <td>{{ entity.reported }}</td>
                <td>{{ entity.legalPaperScan }}</td>
                <td>{{ entity.isActive }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('schools_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('schools_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('schools_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
